# چک‌لیست ضروری Dietory 5.3.0

## پشتیبان خودکار

- [ ] در تنظیمات، محل پشتیبان نمایش داده می‌شود و قابل تغییر است.
- [ ] دکمه «به‌روزرسانی پشتیبان اصلی اکنون» پوشه `Dietory-Auto-Backup-Latest` را می‌سازد.
- [ ] داخل پشتیبان، `nutritionist.sqlite`، `manifest.json` و پوشه‌های فایل وجود دارند.
- [ ] با بستن عادی اپ، پنجره «در حال پشتیبان‌گیری و خروج» نمایش داده می‌شود.
- [ ] پس از خروج، زمان `Dietory-Auto-Backup-Latest` جدید می‌شود.
- [ ] بعد از خروج دوم، نسخه قبلی در `Dietory-Auto-Backup-Previous` وجود دارد.
- [ ] اگر مسیر پشتیبان در دسترس نباشد، اپ بدون هشدار بسته نمی‌شود.

## بازیابی کامل

- [ ] یک مراجع آزمایشی، ویزیت، فایل و تنظیمات ایجاد شود و پشتیبان گرفته شود.
- [ ] داده آزمایشی تغییر کند و سپس پشتیبان کامل Restore شود.
- [ ] تعداد مراجع و ویزیت درست گزارش شود.
- [ ] تصاویر و PDFها بعد از Restore باز شوند.
- [ ] تنظیمات کلینیک و ورود حفظ شوند.
- [ ] پوشه Safety با عنوان `Before-Restore` ساخته شود.

## بروزرسانی استاندارد

- [ ] Artifact شامل `START-HERE-DIETORY.hta`، `UPDATE-CURRENT-APP.vbs` و Setup است.
- [ ] هنگام بازبودن اپ، Update ابتدا اپ را از مسیر بستن عادی می‌بندد و منتظر پشتیبان می‌ماند.
- [ ] `Documents\Dietory Update Backups\pre-update-latest` ساخته می‌شود.
- [ ] گزینه «بروزرسانی نسخه فعلی» نسخه 5.3.0 را نصب می‌کند.
- [ ] مراجعین، ویزیت‌ها، فایل‌ها، نام کلینیک، رنگ‌ها و تنظیمات قبلی پس از Update باقی می‌مانند.
- [ ] نسخه قبلی پیش از Update دستی Uninstall نمی‌شود.

توجه: پشتیبان هنگام بستن عادی اجرا می‌شود؛ خاموشی ناگهانی برق یا خرابی کامل دیسک فقط با انتخاب مسیر خارج از همان دیسک، مانند هارد اکسترنال یا پوشه همگام‌شونده، پوشش داده می‌شود.
